
## 模型切换同步规则（2026-03-27 凌晨，鹏哥明确要求）
- 以后只要鹏哥要求切换 weather-odds-bot 模型，必须**两边一起切**：
  1. 主会话 / agent model config
  2. weather-arb-full-scan cron payload model
- 禁止只切聊天窗口、不切 cron，避免出现“主会话已换模型，但定时任务还在跑旧模型”的不一致。
- 本次已执行：weather-arb-full-scan cron payload 改为 `openai-codex/gpt-5.4`。
- 核查结果：weather-odds-bot agent config 当前也已是 `openai-codex/gpt-5.4`。

## 汇总口径 bug 修复（2026-03-27 早）
- 鹏哥指出 cron 推送中的汇总数字异常：同一条报告里 `持仓5个 / 已平仓11笔` 对得上，但 `现金$288.80 / 累计PnL -13.34` 与 `data/portfolio.json` 原始账面不一致。
- 排查结果：`scripts/sim_portfolio_scan.mjs` 的 summary 仍在使用一套旧的 BUY_NO “纠偏汇总口径”（`getCorrectedSummaryAccounting` / `getCorrectedClosedTradePnl`）。
- 这套纠偏逻辑原本是为了修复过去把 BUY_NO 当成 `1-YES` 处理时的历史汇总误差；但在 BUY_NO 已全面改成 **direct PM NO price** 之后，这套旧纠偏继续存在，反而把 summary 里的 cash / cumulative PnL 算错。
- 已修复：summary 改为直接使用真实账面字段：
  - `cash = pf.cash`
  - `累计PnL = pf.totalPnl`
  - `总资产 = pf.cash + 当前订单簿可平资产`
- 修复后实跑校验通过，最新输出：`持仓4个｜现金$326.54｜可平资产$67.47｜总资产$394.01｜已平仓12笔｜累计PnL $4.58`。
- 结论：以后 Telegram 推送汇总以 `portfolio.json` 实时账面为准，不再走旧纠偏汇总函数。

## 汇总文案升级（2026-03-27 早）
- 鹏哥指出旧 summary 即使账面修正后，`累计PnL` 这个词仍然容易被理解成“总盈亏”，而脚本里实际展示的是“已实现PnL”，会和总资产是否亏损形成认知冲突。
- 已调整 Telegram / cron summary 输出口径，明确拆成：
  - `总资产`
  - `已实现`
  - `浮动`
  - `总PnL`
- 新口径定义：
  - `总资产 = cash + 可平资产`
  - `总PnL = 总资产 - initialCapital`
  - `浮动 = 总PnL - 已实现PnL`
- 实跑校验通过，样例输出：`持仓4个｜现金$326.54｜可平资产$69.57｜总资产$396.11｜已实现$4.58｜浮动$-8.47｜总PnL $-3.89｜已平仓12笔`
- 目的：以后用户一眼就能分清“落袋收益”和“当前总账是否盈利”，避免再出现“总资产是亏的，但累计PnL是正的”这种歧义。
