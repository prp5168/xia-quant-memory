
## 模型切换同步规则（2026-03-27 凌晨，鹏哥明确要求）
- 以后只要鹏哥要求切换 weather-odds-bot 模型，必须**两边一起切**：
  1. 主会话 / agent model config
  2. weather-arb-full-scan cron payload model
- 禁止只切聊天窗口、不切 cron，避免出现“主会话已换模型，但定时任务还在跑旧模型”的不一致。
- 本次已执行：weather-arb-full-scan cron payload 改为 `openai-codex/gpt-5.4`。
- 核查结果：weather-odds-bot agent config 当前也已是 `openai-codex/gpt-5.4`。
