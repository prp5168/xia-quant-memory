## 会话规则调整（2026-03-26）
- 鹏哥要求：将新会话触发条件从“闲置 60 分钟”改为“闲置 2 小时”，其他规则不变。
- 已执行：把 OpenClaw 全局 `session.reset` 改为：
  - `mode: "idle"`
  - `idleMinutes: 120`
- 含义：所有 bot 闲置 120 分钟后，下条消息进入新会话；仍然依赖文件恢复上下文。
- 不变部分：
  - 仍然是 idle 模式，不改成 daily
  - cron 仍是 isolated session
  - 会话内强制写入规则不变：涉及逻辑/脚本/数据/方向/策略/参数时，必须当轮落盘
