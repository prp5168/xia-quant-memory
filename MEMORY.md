# MEMORY.md - 虾赚的长期记忆

## 身份
- 我叫虾赚（🎲），天气套利 bot 的搭建助手
- 用户叫鹏哥（da peng, Telegram @dapengniao1）
- 时区 GMT+8

## 项目：天气套利系统
- 目标：利用 WU 同源预报数据 vs Polymarket 天气温度盘口的赔率差进行套利
- 策略：提前一天在赔率被低估时买入，在赔率被市场发现/高估时卖出（不等结算）
- 城市：动态扫描 Polymarket volume top10 + watchlist（含上海/首尔/伦敦/巴黎/华沙/马德里/惠灵顿/勒克瑙/达拉斯/芝加哥/纽约/迈阿密/多伦多/西雅图等）
- 只扫今天+明天（后天已取消），今天标的只观察不建仓

## 关键数据源
- TWC API（api.weather.com）：WU 同源，apiKey = e1f10a1e78da46f5b10a1e78da96f525，服务器可直连
- Polymarket Gamma API：市场信息/价格
- Polymarket CLOB /book：订单簿深度（公开）
- NOAA AWC：METAR 实时观测（公开）

## 交易规则
- σ = 0.8
- 买入：edge ≥ 20%，盘口价 10%-90%
- 卖出：edge 反转 ≥ 20% 或吃到 60% edge
- 止损：预报偏移 ≥ 2°C / METAR 突破 / 盘口异动（市价单，不犹豫）
- 模拟盘本金 $400（已归零重开）

## 鱼身理论（2026-03-18 鹏哥提出的核心策略思路）
- 赚"提前一天的不确定性赔率差 → 第二天赔率差缩小/反转"的利润
- 只对明天/后天的标的建仓，不做当天对当天
- 当天加密扫描只监控持仓止损/止盈，不建新仓
- 不追鱼头（太早太不确定）、不追鱼尾（临近结算赔率差已消失）
- 目标：长期稳定获利曲线，不追极端收益

## 交易规则（实际执行值，以 portfolio.json rules 为准）
- minEdge = 0.15（15%），不是 MEMORY 之前写的 20%
- σ: 当天 0.8, 明天 1.0, 后天 1.5
- 汇报必须基于脚本实时数据，不靠记忆文件

## 核心认知：目标不是预报准，是精准契合 WU 结算数据
- PM 结算标准 = wunderground.com/history/daily/{station} 当天所有时间最高温（整数°C）
- TWC API historical observations（v1 endpoint）和 WU History 同源同数据
- 所以：TWC API 实测 = WU 结算值（已验证 ZSPD/RKSI/KJFK/EGLL 站点一致）

## 观察模式（2026-03-21 鹏哥决策，当前状态）

### 为什么进入观察模式
- 43笔已平仓，胜率14%，累计PnL -175u，模型信号方向判断大面积失准
- 核心问题：不知道模型算出的 edge 是否真的有预测价值
- 决策：暂停一切建仓，纯观察，先验证再恢复

### 观察模式下必须记录的内容
1. **observe-log.jsonl — 概率路径观察（每轮扫描自动写入）**
   - 每个城市/日期标的的 TWC 预报 max、调整后 mu、sigma
   - **所有** market bins 的 model概率 vs 盘口概率 vs edge（不只是 ±2°C）
   - 是否是当天标的、当地几点
   - 结算日当天 local 16:00 后停止记录（最高温已实现）
   - 目的：追踪"模型说有edge → 后续edge是否收敛 → 结算结果"的完整路径

2. **forecast-log.jsonl — TWC预报误差积累（每轮扫描自动写入）**
   - forecastMax、hourlyMax、mu、sigma、天气参数（风/降水/云/湿度）
   - 提前天数（dayDiff）
   - 目的：积累预报误差样本，验证 sigma 参数是否合理

3. **watchlist.json — 城市追踪延续（每轮扫描自动维护）**
   - volume top10 城市自动写入
   - 有 edge 信号的城市也写入
   - 城市不因 volume 排名波动而丢失观察，直到结算日过期自动清理
   - 目的：解决"昨天发现好 edge 今天 volume 掉了就不看了"的问题

### 观察期要验证的三个核心问题
1. **edge 收敛验证**：模型算出的 edge，后续盘口是否向模型方向收敛？（用 observe-log 追踪同一标的多轮概率变化）
2. **BUY_YES 方向问题**：历史上 BUY_YES 几乎全错，为什么？是模型系统性偏差还是市场定价逻辑不同？
3. **模型 vs 市场维度差异**：市场对精确温度档的定价是否和正态分布+sigma模型在不同维度？

### 已有的回测工具
- `scripts/backtest_twc_accuracy.mjs` — TWC预报 vs WU结算实测对比
- `scripts/backtest_forecast_error.mjs` — 30天预报误差统计（Open-Meteo历史预报 vs TWC实测）
- `scripts/backtest_90d_sigma.mjs` — 90天历史最高温标准差
- `data/forecast_error_30d.json` — 各城市提前1天/2天预报误差（bias/MAE/sigma）
- `data/city_sigma_90d.json` — 各城市90天历史sigma

### 观察期退出条件（待鹏哥确认）
- 需要积累足够样本验证上述三个问题
- 验证通过后再决定是否恢复交易、用什么参数

### 已知bug（已修复/保留）
- BUY_NO 止损路径A（预报偏移止损）的 exitVal 计算有 bug（line 402-403），BUY_NO 用了 exitPrice*shares 而非 (1-exitPrice)*shares，但其他止损路径已正确。鹏哥决定不修，账户归零重开。
- stoppedToday 跨 UTC 午夜清空 bug → 已改为24小时滚动窗口
- 扫描范围已改为今天+明天，后天不扫

### 记忆维护铁律（2026-03-22 鹏哥明确要求）
- 每次重要讨论的结论、决策、逻辑必须写入 MEMORY.md，不能只存日记
- 新开会话不能有重要逻辑/内容遗失
- token 优化不能以丢失关键决策记录为代价
- 日记是流水账，MEMORY.md 是决策手册——两者都要写

## 用户偏好
- 白话输出，不堆术语
- 主模型 claude-opus-4.6，备用 gpt-5.2（不可用时切换并通知）
- 执行原则：严谨、客观、高效，不杜撰不瞎扯，只基于数据说话

## 铁律：数据来源纪律（2026-03-18 鹏哥明确要求）
- 价格/概率判断：只能基于 PM 盘口实时数据（Gamma API / CLOB book）
- 温度预测：只能基于 TWC API（WU 同源）和 NOAA METAR
- 不做任何主观推测，不编数据，不用"大概""可能"模糊带过
- 说不了的就说"当前无数据"
- 表述逻辑能且只能严格基于以上两个数据源

## 铁律：绝对真实（2026-03-18 鹏哥明确要求）
- 所有交易汇报必须 100% 来自 portfolio.json 原始数据，不得手动编造/修饰
- 每次 cron 扫描结果完整转发，不省略、不总结、不美化
- 盈亏如实反映，亏了就说亏了，不找借口不美化
- 任何不确定的地方必须标注"不确定"，不得编造数据填充
- 绝对客观、理性，零杜撰、零吹水
- 这条高于一切其他汇报规则
